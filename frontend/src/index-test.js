// Simple JavaScript file to test if the environment is working
console.log('Hello, world!');

// Export a simple function
function greet(name) {
  return `Hello, ${name}!`;
}

module.exports = { greet };